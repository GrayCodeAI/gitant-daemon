// Package adapters contains implementations that map domain repository interfaces
// to concrete infrastructure implementations (CRDT stores, storage registry, etc.).
// These are "adapters" in the hexagonal architecture.
package adapters

// Placeholder file for the adapters package.
// Actual adapter implementations will be created in Phase 2.
